export const getMilliseconds = date => date.getMilliseconds();
export const getTime = date => date.getTime();
export const getTimezoneOffset = date => date.getTimezoneOffset();
export const setFullYear = (date, year) => date.setFullYear(year);
export const setMonth = (date, month) => date.setMonth(month);
export const setDate = (date, day) => date.setDate(day);
